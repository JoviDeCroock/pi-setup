{
  "name": "@pi-setup/tool-pruner",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that keeps the active tool set intentionally small.",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "clean": "rimraf dist",
    "test": "tsx --test"
  },
  "dependencies": {
    "@pi-setup/pi-kit": "workspace:*"
  }
}
