# Local Project Example

This example shows how to wire the built extensions into a project-scoped `.pi/settings.json`.

From the repository root:

```bash
pnpm build
```

Then open Pi from inside `examples/local-project/` to use the relative extension paths in `.pi/settings.json`.
