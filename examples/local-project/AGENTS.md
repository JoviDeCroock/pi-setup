# Example Project

This example project uses the local `review-gate`, `repo-context`, and `usage-insights` extensions from the workspace build output.

Prefer the extension tools for repository exploration before asking Pi to do broad unstructured scans.
